/* eslint-disable @typescript-eslint/no-var-requires */
const child = require("child_process");

try {
  // child.execSync('git rev-parse --is-inside-work-tree');
  // child.execSync('husky install');
} catch (error) {
  //
}
