{
  "name": "qiankun-demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev:main": "cd main && yarn dev && cd ../react-app && yarn dev:app",
    "dev:app": "cd react-app && yarn dev:app",
    "build": "cd main && yarn build && cd ../react-app && yarn build:app",
    "start": "cd dist && http-server --port=8082 --proxy http://localhost:8082?"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
